<%- include("../partials/header") %>

<div class="flex flex-col items-center min-h-screen bg-white py-10">
  <div class="bg-rose-50 rounded-xl shadow-lg p-8 w-full max-w-md">
    <h2 class="text-2xl font-bold mb-3 text-rose-800 text-center">GCD Calculator</h2>
    <p class="text-gray-700 mb-5 text-center">
      The <span class="font-semibold text-rose-800">Greatest Common Divisor (GCD)</span> of two numbers is the largest
      positive integer that divides both without a remainder.
    </p>

    <form id="gcdForm" class="flex flex-col gap-4">
      <input type="number" id="gcdNum1" placeholder="First number" required
             class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-rose-400" />
      <input type="number" id="gcdNum2" placeholder="Second number" required
             class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-rose-400" />

      <button type="submit"
              class="bg-rose-600 hover:bg-rose-700 transition text-white font-bold py-2 px-4 rounded mt-2">
        Find GCD
      </button>
    </form>

    <div id="gcdResult" class="mt-6 text-xl text-blue-700 text-center"></div>

    <!-- Explanation box -->
    <div id="gcdExplanation" class="mt-6 hidden">
      <div class="bg-rose-100 border-l-4 border-rose-600 p-3 rounded">
        <span class="font-medium text-rose-800 block mb-1">How was this GCD found?</span>
        <p class="text-gray-800 text-sm" id="gcdExplanationText"></p>
      </div>
    </div>

    <div class="mt-6">
      <div class="bg-rose-100 border-l-4 border-rose-600 p-3 rounded">
        <span class="font-medium text-rose-800">Formula:</span>
        <div class="font-mono text-gray-800 text-sm mt-1">
          GCD is found using Euclidean Algorithm
        </div>
      </div>
      <div class="mt-3 flex flex-col gap-2">
        <span class="bg-blue-100 text-blue-900 px-3 py-1 rounded text-xs font-medium shadow">Example: GCD(12, 8) = 4</span>
        <span class="bg-purple-100 text-purple-900 px-3 py-1 rounded text-xs font-medium shadow">Example: GCD(48, 18) = 6</span>
      </div>
    </div>
  </div>
</div>

<%- include("../partials/footer") %>
