<%- include("../partials/header") %>
<div class="flex flex-col items-center min-h-screen bg-white py-10">
  <div class="bg-blue-50 rounded-xl shadow-lg p-8 w-full max-w-md">
    <h2 class="text-2xl font-bold mb-3 text-blue-800 text-center">Factorial Calculator</h2>
    <p class="text-gray-700 mb-5 text-center">
      The <span class="font-semibold text-blue-800">factorial</span> of a number, written <span class="font-bold italic">n!</span>, is the product of all positive integers up to n. Factorials are essential in permutations, combinations, and probability!
    </p>
    <form id="factorialForm" class="flex flex-col gap-4">
      <input type="number" id="factorialInput" min="0" placeholder="Enter a non-negative integer" required class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400"/>
      <button type="submit" class="bg-blue-700 hover:bg-blue-800 transition text-white font-bold py-2 px-4 rounded mt-2">
        Calculate
      </button>
    </form>
    <div id="factorialResult" class="mt-6 text-xl text-green-700 text-center"></div>
    <div class="mt-6">
      <div class="bg-blue-100 border-l-4 border-blue-600 p-3 rounded">
        <span class="font-medium text-blue-800">Formula:</span>
        <span class="font-mono text-gray-800">n! = 1 × 2 × 3 × ... × n</span>
      </div>
      <div class="mt-3 flex justify-center items-center gap-2">
        <span class="bg-yellow-200 text-yellow-900 px-3 py-1 rounded text-xs font-medium shadow">Example: 5! = 1×2×3×4×5 = 120</span>
      </div>
    </div>
    <div class="mt-4 text-gray-500 text-xs text-center italic">
      Try calculating factorial for probability and counting problems!
    </div>
  </div>
</div>
<script src="/public/js/script.js"></script>
<%- include("../partials/footer") %>
